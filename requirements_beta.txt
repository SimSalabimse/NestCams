# Requirements for Bird Box Video Processor v10.0.0
# Compatible with Python 3.8 or later. Python 3.12 is recommended. Python 3.13 requires compatible wheels for some dependencies.

# GUI dependencies
customtkinter==5.2.2      # Modern Tkinter-based GUI library
tkinterdnd2==0.4.3        # Drag-and-drop support for Tkinter

# Image and video processing
opencv-python==4.10.0.84  # OpenCV for video frame processing (compatible with Python 3.13/macOS)
numpy==1.26.4             # Numerical operations for motion detection
Pillow==10.4.0            # Image handling for previews
matplotlib==3.9.2         # Analytics charts (compatible with Python 3.13)

# YouTube API and authentication
google-api-python-client==2.140.0  # YouTube API for video uploads
google-auth-oauthlib==1.2.1        # OAuth2 authentication
google-auth-httplib2==0.2.0        # HTTP client for Google APIs
uritemplate==4.1.1                 # URI template parsing for Google APIs

# Network and system utilities
requests==2.32.3      # HTTP requests for update checks
psutil==6.0.0         # System resource monitoring (CPU, RAM)

# Scheduling and version management
schedule==1.2.2       # Simple scheduling for automated tasks
packaging==24.1       # Version parsing for update checks

# Optional: Network speed testing
speedtest-cli==2.1.3  # For upload speed checks (optional, install manually if needed)